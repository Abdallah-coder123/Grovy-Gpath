def query=data.course.name
print query//[[selenium,Appium],[selenium,Postman]] بجيب كل name inside array
__________________________________________

pm.test("Verify all object",()=>{
    _.each(jsonData.items,(el)=>{
        pm.expect(el.caseTypeName).to.include("Remittance")
    })
})
